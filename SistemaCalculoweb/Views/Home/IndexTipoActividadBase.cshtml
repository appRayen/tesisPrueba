@model IEnumerable<SistemaCalculoweb.TipoActividadBase>

@{
    ViewBag.Title = "IndexTipoActividadBase";
}

<h2>IndexTipoActividadBase</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.estado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.estado)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idTipoActividadBase }) |
            @Html.ActionLink("Details", "Details", new { id=item.idTipoActividadBase }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idTipoActividadBase })
        </td>
    </tr>
}

</table>
